import { FitViewOptions } from "@xyflow/react";

export const CANVAS_VIEW_SETTINGS : FitViewOptions = {
    padding: {
        left: "280px",
        right: "40px",
      },
      maxZoom: 1,
      duration: 1000,
}