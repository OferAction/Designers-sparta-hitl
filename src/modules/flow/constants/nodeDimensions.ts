/**
 * Constants related to node dimensions and positioning in the flow editor
 */

// Default node dimensions
export const DEFAULT_NODE_WIDTH = 750;
export const DEFAULT_NODE_HEIGHT = 750;

// Padding constants
export const NODE_LEFT_PADDING = 50;
export const NODE_RIGHT_PADDING = 50;
export const NODE_TOP_PADDING = 90;
export const NODE_BOTTOM_PADDING = 50;

// Minimum node dimensions
export const MIN_NODE_HEIGHT = 180;
export const MIN_NODE_WIDTH = 150;

// For extent calculations
export const NODE_EXTENT_LEFT = NODE_LEFT_PADDING;
export const NODE_EXTENT_TOP = NODE_TOP_PADDING;
export const NODE_EXTENT_RIGHT_OFFSET = NODE_RIGHT_PADDING;
export const NODE_EXTENT_BOTTOM_OFFSET = NODE_BOTTOM_PADDING;

// For resize calculations
export const NODE_HORIZONTAL_PADDING = NODE_LEFT_PADDING + NODE_RIGHT_PADDING;
export const NODE_VERTICAL_PADDING = NODE_TOP_PADDING + NODE_BOTTOM_PADDING;
